# MCP guidelines

MCP tools are prohibited unless user explicitly opts in to using them.

## chrome-devtools-mcp
- always use `new_page` with `isolatedContext` to prevent conflicts betweewn sessions


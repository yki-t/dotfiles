# Codex

- 回答は敬体の日本語で書く。技術用語と識別子は原文のままにする。
- サブエージェントは `~/.codex/agents/` に定義する。rules 内の `code-reviewer` は `code_reviewer`、`subagents-checker` は `subagents_checker` を指す。
- rules 内の skill 名（`/dev` など）は `$dev` のように `$` 付きで呼び出す。

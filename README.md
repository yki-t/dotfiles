# dotfiles

## Get Started

```bash
git clone https://github.com/yki-t/dotfiles ~/dotfiles
```

## Apply zsh setting

```bash
ln -snf dotfiles/.zprofile ~/
ln -snf dotfiles/.zshrc ~/

source ~/.zprofile
source ~/.zshrc
```

